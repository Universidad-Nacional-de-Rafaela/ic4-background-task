# Ejemplo de gestión de tareas en segundo plano.

Usamos Python, FastAPI, Redis, Celery.

## Para levantar la infraestructura.

```
# ingresar el directorio app
cd ./app

# ejecutar docker
docker compose up --build
```
